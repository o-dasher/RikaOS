local tw = 2

vim.bo.tabstop = tw -- size of a hard tabstop (ts).
vim.bo.shiftwidth = tw -- size of an indentation (sw).
vim.bo.softtabstop = tw -- number of spaces a <Tab> counts for. When 0, feature is off (sts).
vim.bo.expandtab = true -- always uses spaces instead of tab characters (et).
